node_modules/
dist/
*.tsbuildinfo
.DS_Store

# Derived cache — never source of truth (ADR-002)
.vbd/
workspaces/*/.vbd/

# Server-side project data (ADR-006) — user workspaces, not versioned in this repo
data/

# Local env / secrets
.env
.env.*

# Generated serverless functions (bundled from apps/web/functions by build-functions.mjs)
apps/web/api/
